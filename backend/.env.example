# Database Configuration
DB_URL=jdbc:mysql://localhost:3306/ecommerce_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=password

# JWT Configuration
# IMPORTANT: Generate a strong, random secret key for production (minimum 64 characters)
# You can generate one using: openssl rand -base64 64
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_SECRET_KEY_AT_LEAST_64_CHARACTERS_LONG_FOR_PRODUCTION_USE_ONLY
JWT_EXPIRATION=86400000

# Server Configuration
SERVER_PORT=8080
